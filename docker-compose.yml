services:
  app:
    build: .
    ports:
      - "8888:8888"
    volumes:
      - ./data:/app/data
      - ./notebooks:/app/notebooks
      - ./outputs:/app/outputs
      - ./src:/app/src
      - ./configs:/app/configs
